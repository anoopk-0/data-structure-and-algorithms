/**
 * An object is an unordered collection of key-value pairs. The key must either be a string or symbol data type where as the value can be any data type.
 *
 * An object is not an iterable. cannot be use it as a for of loop.
 *
 * IMP:Methods
 *  - Object.keys .values and .entires
 *
 * Array Big O time complexity
 *  - inset/remove  - o(1)
 *  - access - O(1)
 *  - search - O(n)
 *
 *  methods
 *  Object.keys .values and entries - O(n)
 */
